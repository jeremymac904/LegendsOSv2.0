# dependencies
node_modules
.pnp
.pnp.js
.yarn/install-state.gz

# testing
coverage
playwright-report
test-results
.playwright-mcp/

# next.js
.next
out
next-env.d.ts

# production
build
dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env*.local
.env
netlify-production-import.env

# vercel / netlify
.vercel
.netlify

# typescript
*.tsbuildinfo

# tool noise (this project uses Node + npm, not Deno)
deno.lock

# supabase
supabase/.branches
supabase/.temp

# editor
.idea
.vscode
*.swp
*.swo

# Claude Code repo-local config — ignore the directory by default, but
# explicitly track the shared multi-agent system (agents + commands).
# Per-machine bits (settings.local.json, launch.json, scheduled_tasks.lock)
# stay ignored.
.claude/*
!.claude/agents/
!.claude/agents/**
!.claude/commands/
!.claude/commands/**

# logs
logs
*.log

# OS
Thumbs.db

# Local-only workspace folders (Jeremy's drag-drop staging — not part of the app)
/future/
/images/
/videos/

# Source pack lives extracted; ignore the redundant zip
/LegendsOS_v2_Execution_Command_Pack.zip
